# only listed properties are used by the system

[project]
name = "ad-factory-agency"
description = "UGC Ad Factory – AI-powered short-form video ad creation agency"
version = "0.1.0"
license = { text = "MIT" }
keywords = ["agency-swarm", "ugc", "video-ads", "agencii"]

[tool.template]
version = "0.0.2"
